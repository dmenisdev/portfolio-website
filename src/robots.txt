User-agent: *
Disallow: /private/
Disallow: /drafts/
Disallow: /todo/
Allow: /

# Optional: prevent indexing of specific file types
Disallow: /*.psd$
Disallow: /*.zip$
Disallow: /*.bak$

# Optional: delay for crawlers to avoid stressing your server (for small servers)
# Crawl-delay: 10

# Optional: Sitemap location (update if you generate a sitemap)
# Sitemap: https://wbroek.pages.dev/sitemap.xml
